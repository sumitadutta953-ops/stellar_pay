[workspace]
members = [
    "payment-hub",
    "payment-validator",
    "escrow",
    "arbitration",
]
resolver = "2"

[profile.release]
opt-level = "z"
overflow-checks = true
lto = true
codegen-units = 1
panic = "abort"
