[package]
name = "payment-validator"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { version = "21.6.0", features = ["testutils"] }

[dev-dependencies]
soroban-sdk = { version = "21.6.0", features = ["testutils"] }
